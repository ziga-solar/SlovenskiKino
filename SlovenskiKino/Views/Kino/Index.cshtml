@model IEnumerable<SlovenskiKino.Models.Kinematografi>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Kinematograf)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdPodjetjaNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Kinematograf)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdPodjetjaNavigation.IdPodjetja)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdKinematograf">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdKinematograf">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdKinematograf">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
